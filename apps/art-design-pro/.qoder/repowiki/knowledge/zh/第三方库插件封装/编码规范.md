- 每个第三方库独立一个文件，通过 `index.ts` 统一 `export *` 暴露，避免外部直接引用子模块路径。
- ECharts 严格遵循按需导入模式：从 `echarts/core`、`echarts/charts`、`echarts/components`、`echarts/renderers` 分别引入，再统一注册到 `echarts.use()`。
- Vue 插件以 `initXxx(app: App): void` 函数形式暴露，由应用启动阶段调用 `app.use` 完成安装。