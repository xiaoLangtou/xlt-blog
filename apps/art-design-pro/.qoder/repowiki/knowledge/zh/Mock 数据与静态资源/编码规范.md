- 每个数据文件先声明 TypeScript interface，再以大写常量名（如 `ACCOUNT_TABLE_DATA`、`ROLE_LIST_DATA`、`ORG_DATA`）导出对应数组或对象。
- 组织架构数据采用「原始数组 + Map 索引」双结构：先定义 `DEPARTMENTS`、`EMPLOYEES` 常量，再在模块顶层循环构建 `deptMap`、`deptChildrenMap`、`deptEmployeeMap` 供查询使用。
- 需要响应式的 Mock 数据统一通过 Vue `ref` 包裹后导出（如 `upgradeLogList`），而非普通常量。