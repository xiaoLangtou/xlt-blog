- 样式文件采用 SCSS @use 语法进行模块化导入，由 index.scss 作为唯一聚合入口。
- 主题色与布局尺寸通过 CSS 自定义属性（如 --theme-color、--default-box-color、--art-card-border）定义，避免硬编码颜色值。
- 暗黑主题通过为 html 元素添加 .dark 类并覆盖对应 CSS 变量实现，而非创建独立样式文件。
- 图片资源按业务场景分目录存放（avatar、ceremony、settings/menu_styles 等），命名以功能语义为主。