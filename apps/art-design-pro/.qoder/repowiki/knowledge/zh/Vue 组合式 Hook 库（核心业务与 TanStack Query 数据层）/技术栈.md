Vue 3 Composition API + TypeScript；状态管理使用 Pinia；HTTP 请求由 `@tanstack/vue-query` 驱动（`useQuery`/`useMutation`）；UI 辅助来自 `@vueuse/core`（如 `useWindowSize`、`usePreferredDark`）；表格方案自研并复用 `src/utils/table/` 下的 `tableCache`、`tableUtils`、`tableConfig`。