- queryKey 采用「域.all 基数组 + 函数式派生」模式，所有具体 key 都通过展开 base 数组并追加标识符的方式生成，保证类型安全与一致性。
- 每个业务域的 key 工厂都以 `all` 属性作为命名空间前缀，子方法通过 `[...queryKeys.<domain>.all, ...]` 继承前缀。
- queryKey 函数返回值使用 `as const` 断言，确保 key 数组元素类型精确推导。
- 模块通过 index.ts 做扁平化 re-export，对外只暴露必要 API，内部实现细节不直接暴露。