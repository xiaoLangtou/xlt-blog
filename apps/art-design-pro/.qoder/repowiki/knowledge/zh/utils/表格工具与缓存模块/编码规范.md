- 所有可配置的字段名均以数组形式集中声明在 `tableConfig.ts` 中，按优先级顺序排列，便于多后端兼容。
- 对外暴露的类型均通过 TypeScript 接口定义（如 `ApiResponse<T>`、`CacheItem<T>`、`TableError`），并在模块顶部以 JSDoc 注释说明用途。
- 工具函数采用纯函数设计，不持有内部状态，通过参数传入配置并返回标准化结果。
- 日志输出统一通过可选的 `enableLog` 开关控制，避免生产环境冗余输出。