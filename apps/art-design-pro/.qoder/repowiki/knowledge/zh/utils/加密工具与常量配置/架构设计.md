模块由两个并列的子目录组成：`crypto/` 仅包含 `index.ts`，通过导入 `crypto-js` 暴露 `md5` 和 `encryptPassword` 两个纯函数；`constants/` 采用 barrel 导出模式，`index.ts` 统一 re-export `links.ts` 中的 `WEB_LINKS` 对象，将分散的 URL 常量聚合为单一入口。依赖方向单向：业务代码仅依赖这两个子模块的导出，不反向引用。