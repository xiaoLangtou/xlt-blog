- 组件统一通过 `defineOptions({ name: '...' })` 显式声明组件名称，便于调试和路由匹配。
- 视图组件采用 `<script setup lang="ts">` 语法，配合响应式 `ref` 管理本地状态（如 `isLoading`、`iframeUrl`）。
- 样式使用 SCSS 嵌套 + CSS 变量（如 `--default-bg-color`、`--default-box-color`）统一管理主题色与间距。
- 外部资源嵌入通过路由 `meta.link` 配置驱动，视图只负责读取并渲染，不硬编码 URL。