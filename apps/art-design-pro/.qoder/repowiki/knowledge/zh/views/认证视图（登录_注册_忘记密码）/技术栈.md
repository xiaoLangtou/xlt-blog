Vue 3 Composition API（`<script setup lang="ts">`）、Element Plus（`ElForm`、`ElFormItem`、`ElInput`、`ElButton`、`ElCheckbox`、`ElNotification`）、vue-i18n 国际化、Tailwind CSS（通过 `@reference '@styles/core/tailwind.css'` 引用）、ArtSvgIcon 图标组件。