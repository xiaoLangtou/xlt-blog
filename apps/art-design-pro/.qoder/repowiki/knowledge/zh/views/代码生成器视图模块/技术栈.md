Vue 3 `<script setup>` + TypeScript；UI 使用 Element Plus（ElCard、ElTable、ElTag、ElInput 等）；表格能力来自 @tanstack/vue-table（headless 模式，配合 FlexRender 渲染）；样式采用 scoped SCSS 并复用项目主题变量（如 --art-gray-*、--theme-color）。