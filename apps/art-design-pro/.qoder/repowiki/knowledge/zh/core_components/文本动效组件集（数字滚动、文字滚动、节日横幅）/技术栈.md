Vue 3 `<script setup lang="ts">` + TypeScript 接口；动画核心依赖 `@vueuse/core`（`useTransition`、`useRafFn`、`useElementSize`、`useElementHover`、`useDebounceFn`、`useTimeoutFn`）；样式采用 Tailwind CSS 原子类（如 `flex-c`、`whitespace-nowrap`、`color-mix`）。