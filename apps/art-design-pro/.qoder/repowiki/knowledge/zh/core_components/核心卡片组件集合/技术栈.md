Vue 3 `<script setup lang="ts">` + TypeScript 接口定义；ECharts 通过 `@/plugins/echarts` 引入并使用 `useChartComponent` hook 封装；UI 基于 Element Plus（ElImage、ElProgress、ElScrollbar、ElTimeline、ElButton 等）和自定义 ArtSvgIcon、ArtCountTo 组件；样式采用 Tailwind CSS 原子类。