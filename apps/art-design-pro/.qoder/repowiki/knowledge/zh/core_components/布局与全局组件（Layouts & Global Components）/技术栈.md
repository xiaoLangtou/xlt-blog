Vue 3 `<script setup>` + TypeScript；UI 基于 Element Plus（ElMenu、ElScrollbar、ElDropdown、ElPopover、ElTooltip）；状态管理使用 Pinia store（setting/user/menu/worktab）；国际化 via vue-i18n；窗口尺寸与全屏能力来自 @vueuse/core；样式使用 SCSS 并通过 `:deep()` 覆盖 Element Plus 默认样式。