- 新增语言需同时添加对应 JSON 文件并在 index.ts 的 messages 对象与 languageOptions 中注册，保持 en.json 与 zh.json 结构完全一致。
- 从存储读取用户偏好时使用 try/catch 包裹 JSON.parse，失败时降级到默认语言并输出 console.warn/debug 日志。
- 对外暴露的翻译函数统一通过 `$t` 命名导出，类型为 `(key: string) => string`，避免组件内重复导入 useI18n。