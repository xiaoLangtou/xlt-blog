- 应用初始化采用「先 createApp，再逐个 initXxx(app) 调用」的顺序模式，每个子系统通过独立的初始化函数暴露给入口组装。
- 第三方无类型声明的库统一在 `src/env.d.ts` 中以 `declare module` 形式集中声明类型，避免在各处重复定义。
- 根组件的生命周期初始化按 `onBeforeMount` → `onMounted` 分层：前者负责 UI 准备（过渡、主题），后者负责运行时检查（存储兼容、系统升级）。